sudo: required
language: generic 
services: 
    - docker

before_install:
    - docker build -t patrickjacob/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true patrickjacob/docker-react npm run test -- --coverage